# Producao inicial no VPS. Escrito a mao de proposito: o output de deploy do Aspire mira
# Azure/Kubernetes, e num no unico voce quer um compose legivel as 2h da manha.
# Ver docs/adr/0011-aspire-e-deploy.md.
#
# Uso:  TAG=<sha> docker compose --env-file .env up -d

services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./web-dist:/srv/web:ro        # build estatico do Angular; sem container Node
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api

  api:
    image: ghcr.io/matheusd0mingos/mamao-api:${TAG:?defina TAG}
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__mamao: ${DB_CONNECTION:?}
      Jwt__SigningKey: ${JWT_SIGNING_KEY:?}
      Jwt__Issuer: mamao
      Jwt__Audience: mamao
      Cors__Origins__0: ${PUBLIC_ORIGIN:?}
      App__PublicOrigin: ${PUBLIC_ORIGIN:?}
      Smtp__Host: ${SMTP_HOST:-}
      Smtp__Port: ${SMTP_PORT:-587}
      Smtp__UseStartTls: ${SMTP_USE_STARTTLS:-true}
      Smtp__Username: ${SMTP_USERNAME:-}
      Smtp__Password: ${SMTP_PASSWORD:-}
      Smtp__FromAddress: ${SMTP_FROM_ADDRESS:-nao-responda@mamao.tech}
      Smtp__FromName: ${SMTP_FROM_NAME:-Mamao}
      Smtp__ReplyTo: ${SMTP_REPLY_TO:-}
      DataProtection__KeysPath: /var/mamao/keys
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_ENDPOINT:-}
    volumes:
      - uploads:/var/mamao/uploads
      # As chaves assinam os tokens de recuperacao de senha. Em volume porque, sem
      # persistir, todo restart invalida os links ja enviados.
      - keys:/var/mamao/keys
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz/ready"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    # Postgres sem limite consumindo tudo e o OOM killer matando a API e o incidente
    # classico de VPS. Ajuste conforme o plano contratado.
    mem_limit: 768m
    depends_on:
      postgres:
        condition: service_healthy

  worker:
    image: ghcr.io/matheusd0mingos/mamao-worker:${TAG:?defina TAG}
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__mamao: ${DB_CONNECTION:?}
      Jwt__SigningKey: ${JWT_SIGNING_KEY:?}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_ENDPOINT:-}
    volumes:
      - uploads:/var/mamao/uploads
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 60s        # o Worker aplica as migrations antes de responder
    mem_limit: 512m
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: mamao
      POSTGRES_USER: ${POSTGRES_USER:-mamao_owner}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/10-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mamao_owner} -d mamao"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 1g

volumes:
  pgdata:
  uploads:
  keys:
  caddy_data:
  caddy_config:
